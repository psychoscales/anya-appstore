[project]
name = "anya-appstore-manifestgen"
version = "0.1.0"
description = "Generate dist/manifest.json and version tarballs for anya-appstore"
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6.0.1",
]
