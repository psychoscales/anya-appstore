# Vibe Coding（中文 Demo）

这是一个 **应用介绍 Demo**（非 Compose 应用），用于验证 Anya Client 对 AppStore 文档能力的支持：

- 默认展示版本目录下的 `README.md`
- 支持 `docs.yml` 配置导航（多个 Markdown 页面）
- 支持在 Markdown 中引用 `attachments/` 下的图片等资源

具体内容见 `0.0.1/`。

